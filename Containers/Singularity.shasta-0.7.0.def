BootStrap: docker
From: ubuntu:16.04

%labels
Maintainer Julie Orjuela IRD
base.image="ubuntu:16.04"
version="1"
software="shasta_0.7.0"
software.version="1"

%help
URL: https://chanzuckerberg.github.io/shasta/index.html
Description:  The Shasta long read assembler is to rapidly produce accurate assembled sequence using as input DNA reads generated by Oxford Nanopore flow cell.
Launch the command: singularity run  /PATH_TO_CONTAINER/shasta-0.7.0.simg +  arguments

%post
apt update
apt install -y build-essential wget zlib1g-dev cmake git curl

cd /usr/local

# Download the executable for the latest release.
curl -O -L https://github.com/chanzuckerberg/shasta/releases/download/0.7.0/shasta-Linux-0.7.0

# Grant necessary permissions.
chmod ugo+x shasta-Linux-0.7.0

# symbolic link to rename shasta without version
ln -s shasta-Linux-0.7.0 shasta

# Run an assembly.
#./shasta --input input.fasta

%environment
export  PATH=$PATH:/usr/local/
