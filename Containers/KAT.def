Bootstrap : docker
From : ubuntu:18.04

%labels
Maintainer aurore.comte@ird.fr
KAT

%post
  apt-get -y update
  apt-get -y install sudo wget
  apt-get -y install build-essential curl file git gcc libtool libpthread-stubs0-dev zlib1g-dev
  apt-get -y install autoconf automake autotools-dev python3-dev python3-pip gnuplot gnuplot-x11

  python3 -m pip install sphinx
  python3 -m pip install tabulate
  python3 -m pip install numpy
  python3 -m pip install scipy
  python3 -m pip install matplotlib

  wget https://github.com/TGAC/KAT/archive/Release-2.4.2.tar.gz
  tar -xzf Release-2.4.2.tar.gz
  cd KAT-Release-2.4.2
  ./build_boost.sh
  ./autogen.sh
  #./configure --disable-pykat
  ./configure
  make
  #make check
  make install
  #python3 scripts/setup.py install

%environment
    export PATH=$PATH:/bin
